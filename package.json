{
  "name": "hcmc-history-museum-digital-experience",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=22.13.0 <23 || >=24.0.0 <25",
    "pnpm": "11.18.0"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "turbo run lint && eslint eslint.config.mjs scripts",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "check:ts": "prettier --check . && turbo run lint --force && eslint eslint.config.mjs scripts && turbo run typecheck --force && turbo run test --force && turbo run build --force",
    "check:python": "node scripts/check-python.mjs",
    "check": "prettier --check . && turbo run lint --force && eslint eslint.config.mjs scripts && turbo run typecheck --force && turbo run test --force && turbo run build --force && node scripts/check-python.mjs"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "22.20.1",
    "eslint": "10.8.0",
    "prettier": "3.9.6",
    "turbo": "2.10.8",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0"
  }
}
